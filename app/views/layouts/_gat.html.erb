
  <script>
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
       (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new
       Date();a=s.createElement(o),
       m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
       })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

ga('create', '', '');

<% if flash[:just_signed_up] %>
  var metricValue = '1';
  ga('set', 'metric1', metricValue);
  var dimensionValue = '1';
  ga('set', 'dimension1', dimensionValue);
<% end %>
<%#metrica usuarios registrados en por facebook elsif request.fullpath == language_users_path %>

ga('send', 'pageview');

</script>



<% if not current_user.nil? %>
<script type="text/javascript">
  var _cio = _cio || [];
  (function() {
    var a,b,c;a=function(f){return function(){_cio.push([f].
    concat(Array.prototype.slice.call(arguments,0)))}};b=["load","identify",
    "sidentify","track","page"];for(c=0;c<b.length;c++){_cio[b[c]]=a(b[c])};
    var t = document.createElement('script'),
        s = document.getElementsByTagName('script')[0];
    t.async = true;
    t.id    = 'cio-tracker';
    t.setAttribute('data-site-id', 'f77a207194bb09719221');
    t.src = 'https://assets.customer.io/assets/track.js';
    s.parentNode.insertBefore(t, s);
  })();
</script>

<script type="text/javascript">//Costumer.io
  _cio.identify({
    // Required attributes
    id: "<%= current_user.id %>",           // Unique id for the currently signed in user in your application.
    email: "<%= current_user.email %>", // Email of the currently signed in user.
    created_at: <%= current_user.created_at.to_time.to_i
 %>,   // Timestamp in your system that represents when
                              // the user first signed up. You'll want to send it
                              // as seconds since the epoch.

    // Optional (these are examples. You can name attributes what you wish)

    first_name: "<%= current_user.name %>",       // Add any attributes you'd like to use in the email subject or body.
    surname: "<%= current_user.surname %>",       // Add any attributes you'd like to use in the email subject or body.
    plan_name: "free"      // To use the example segments, set this to 'free' or 'premium'.
  });
</script>
<% end %>